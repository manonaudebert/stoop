# Test/dev dependencies for the whole Python side.
#
# Pulls in ingest/ as well as api/: tests/test_sync_ny.py and
# tests/test_sync_sf.py put ../ingest on sys.path and import the sync modules
# directly, so pandas, tenacity and the rest are needed to COLLECT the api
# suite, not just to run the ingest scripts.
-r requirements.txt
-r requirements-briefs.txt
-r ../ingest/requirements.txt
pytest==9.0.3
# 0.24.0 caps at pytest<9 and cannot be installed beside the pin above; the
# 1.x line is the release that supports pytest 9.
pytest-asyncio==1.4.0
# httpx is not pinned here: it is the async test client AND a direct import in
# ingest and services/briefs/providers.py, so it is pinned once in those files.
# A second pin at a different patch made this file uninstallable.
